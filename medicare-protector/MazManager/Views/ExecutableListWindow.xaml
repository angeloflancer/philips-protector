<Window x:Class="MazManager.Views.ExecutableListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Philips License" 
        Height="480" Width="620"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Status Cell Template -->
        <DataTemplate x:Key="StatusCellTemplate">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource StatusNormalBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                    <Setter Property="Fill" Value="{StaticResource StatusRunningBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Critical">
                                    <Setter Property="Fill" Value="{StaticResource StatusCriticalBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Normal"/>
                            <Setter Property="Foreground" Value="{StaticResource StatusNormalBrush}"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                    <Setter Property="Text" Value="Running"/>
                                    <Setter Property="Foreground" Value="{StaticResource StatusRunningBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Critical">
                                    <Setter Property="Text" Value="Encrypted"/>
                                    <Setter Property="Foreground" Value="{StaticResource StatusCriticalBrush}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <!-- Main Border -->
    <Border Background="#F5F5F5" BorderBrush="#707070" BorderThickness="1">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" 
                    Background="#2196F3"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Philips License" 
                               Foreground="White" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" 
                               Margin="12,0,0,0"/>
                    
                    <!-- Window Controls -->
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right" 
                                VerticalAlignment="Center"
                                Margin="0,0,4,0">
                        <Button x:Name="CloseWindowButton" 
                                Content="X" 
                                Width="32" Height="28"
                                FontWeight="Bold"
                                Style="{StaticResource CloseButtonStyle}"
                                Click="CloseWindowButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Protected Executables" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Add executables to monitor for license protection" 
                                   FontSize="11" 
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>

                <!-- List Card -->
                <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ListView x:Name="ExecutableList" 
                                  Grid.Row="0"
                                  BorderThickness="1"
                                  BorderBrush="{StaticResource BorderBrush}"
                                  Background="White"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListView.View>
                                <GridView AllowsColumnReorder="False">
                                    <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="Full Path" Width="340" DisplayMemberBinding="{Binding FullPath}"/>
                                    <GridViewColumn Header="Status" Width="102" CellTemplate="{StaticResource StatusCellTemplate}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <StackPanel Grid.Row="1" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Left"
                                    Margin="0,10,0,0">
                            <Button x:Name="AddButton" 
                                    Content="Add Executable"
                                    Style="{StaticResource SuccessButton}"
                                    Padding="10,6"
                                    FontSize="11"
                                    Click="AddButton_Click"/>

                            <Button x:Name="RemoveButton" 
                                    Content="Remove Selected"
                                    Style="{StaticResource DangerButton}"
                                    Padding="10,6"
                                    FontSize="11"
                                    Margin="8,0,0,0"
                                    Click="RemoveButton_Click"/>

                            <Button x:Name="RefreshStatusButton" 
                                    Content="Refresh Status"
                                    Style="{StaticResource ModernButton}"
                                    Padding="10,6"
                                    FontSize="11"
                                    Margin="8,0,0,0"
                                    Click="RefreshStatusButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Footer Buttons -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="StatusText" 
                               Grid.Column="0"
                               Text="" 
                               FontSize="10"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>

                    <Button x:Name="SaveButton" 
                            Grid.Column="1"
                            Content="Save Changes"
                            Style="{StaticResource SuccessButton}"
                            Padding="12,8"
                            FontSize="11"
                            Click="SaveButton_Click"/>

                    <Button x:Name="CloseButton" 
                            Grid.Column="3"
                            Content="Close"
                            Style="{StaticResource SecondaryButton}"
                            Padding="12,8"
                            FontSize="11"
                            Click="CloseButton_Click"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
