<Window x:Class="philips_protector.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Required" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Please enter the password to continue:" 
                   FontSize="14" FontWeight="SemiBold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Password:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="0,0,0,5">
                <PasswordBox x:Name="PasswordBox" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             KeyDown="PasswordBox_KeyDown"
                             Visibility="Visible"/>
                <TextBox x:Name="PasswordTextBox" 
                         TextChanged="PasswordTextBox_TextChanged"
                         KeyDown="PasswordTextBox_KeyDown"
                         Visibility="Collapsed"/>
            </Grid>
            <TextBlock x:Name="ErrorMessageTextBlock" 
                       Foreground="Red" 
                       TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Height="20"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,15">
            <CheckBox x:Name="ShowPasswordCheckBox" 
                      Content="Show password" 
                      Checked="ShowPasswordCheckBox_Checked"
                      Unchecked="ShowPasswordCheckBox_Unchecked"
                      Margin="0,0,0,5"/>
            <CheckBox x:Name="RememberMeCheckBox" 
                      Content="Remember me"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" 
                    Width="80" Height="30" 
                    Margin="0,0,10,0"
                    Click="OkButton_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" 
                    Width="80" Height="30" 
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
